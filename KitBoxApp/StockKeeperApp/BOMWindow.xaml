<Window x:Class="StockKeeperApp.BOMWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockKeeperApp"
        mc:Ignorable="d"
        Title="BOMWindow" Height="450" Width="800">
    <Grid Background="{StaticResource BackbroungColor}">
        <DataGrid ItemsSource="{Binding Path=Order.Components}" SelectionMode="Single" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="1.5*" IsReadOnly="True" Binding="{Binding Path=Value}"/>
                <DataGridTextColumn Header="Name" Width="2*" IsReadOnly="True" Binding="{Binding Path=Name}"/>
                <DataGridCheckBoxColumn></DataGridCheckBoxColumn>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding SelectOrderCommand}" />
            </DataGrid.InputBindings>
        </DataGrid>
    </Grid>
</Window>
